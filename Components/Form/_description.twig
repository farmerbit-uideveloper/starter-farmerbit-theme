<div class="{{ (options.description_cols_classes) ? options.description_cols_classes : 'col-md-6' }}">

	{% if descriptionHtml|length %}

		<div class="fc-block__description {{ descriptionAlignment }}">

			{{ descriptionHtml|e('wp_kses_post') }}

		</div>

	{% endif %}
	
</div>