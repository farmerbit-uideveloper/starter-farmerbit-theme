import $ from 'jquery';
// var Instafeed = require("../../node_modules/instafeed.js");

// if( $( '#ig-feed' ).length && $( '#ig-feed' ).is( ':visible' ) ) { 

// 	var feed = new Instafeed({
// 		accessToken: '',
// 		limit: 7, 
// 		template: '<div class="ig-feed__item"><a rel="nofollow" class="ig-feed__link" href="{{link}}" target="_blank"></a><img class="ig-feed__image of-cover" src="{{image}}" alt="{{caption}}"></div>',
// 		imageSize: 640,
//     });

//     feed.run();

// };