<div class="flyntComponent" is="flynt-list-search-results">

    <div class="container-lg">

        <div class="section-taller">

            {% include 'Partials/_header.twig' %}
            {% include 'Partials/_results.twig' %}

            {% if pagination %}
            	{% include 'Partials/_pagination.twig' %}
            {% endif %}
			
        </div>

    </div>

</div>
